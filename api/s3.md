# File Sign API

All endpoints require:

```text
Authorization: Bearer <access_token>
```

Successful responses use the common response envelope:

```json
{
  "code": 0,
  "data": {},
  "message": "Success"
}
```

Error responses use:

```json
{
  "code": -2,
  "message": "invalid request parameters"
}
```

## Storage Layout

Configured buckets:

```text
public_bucket:
  avatars/{user_scope}

private_bucket:
  users/{user_scope}/images/{sha256}.{ext}
  users/{user_scope}/docs/{sha256}.{ext}
```

Storage provider modes:

```toml
[s3]
provider = "s3" # or "cloudflare_r2"
public_bucket = "..."
private_bucket = "..."
```

`provider = "s3"` keeps the original URL shape: `{endpoint}/{bucket}/{key}`.

`provider = "cloudflare_r2"` keeps the same two-bucket layout. Signed S3 API
requests still use `{endpoint}/{bucket}/{key}`. Each bucket can also have its
own bound Cloudflare domain without the bucket name in the URL. Usually only
the public bucket is configured this way:

```toml
public_bucket_url = "https://assets.example.com"
```

For example, public objects can be accepted as
`https://assets.example.com/avatars/{user_scope}`. Private objects should use
the signed access endpoint unless `private_bucket_url` is explicitly configured.

`user_scope` is derived from the JWT subject. When the subject is a UUID, the UUID `u128` value is encoded as Base62.

Clients must not submit S3 paths. The server owns bucket and key generation.

## GET /file/sign/upload/avatar

Returns a PUT signature for the current user's public avatar object.

The avatar object key is stable, so uploading a new avatar overwrites the old one.

Query parameters: none.

Response `data`:

```json
{
  "method": "PUT",
  "upload_url": "http://127.0.0.1:9000/public-bucket/avatars/{user_scope}",
  "key": "avatars/{user_scope}",
  "already_uploaded": false,
  "headers": {
    "authorization": "AWS4-HMAC-SHA256 ...",
    "x-amz-date": "20260520T000000Z",
    "x-amz-content-sha256": "UNSIGNED-PAYLOAD",
    "Content-Type": null,
    "Content-Disposition": "inline"
  }
}
```

## GET /file/sign/upload/image

Returns a PUT signature for a private image.

Query parameters:

| Name | Required | Description |
| --- | --- | --- |
| `filename` | yes | File SHA-256 digest as 64 lowercase or uppercase hex chars. A trailing `.sha256` suffix is accepted and removed. |
| `ext` | yes | One of `jpg`, `jpeg`, `png`, `webp`, `gif`. A leading `.` is accepted and removed. |

The server checks whether the object already exists in S3. If it exists, `already_uploaded` is `true`; the client can skip uploading.

Response `data`:

```json
{
  "method": "PUT",
  "upload_url": "http://127.0.0.1:9000/private-bucket/users/{user_scope}/images/{sha256}.png",
  "key": "users/{user_scope}/images/{sha256}.png",
  "already_uploaded": false,
  "headers": {
    "authorization": "AWS4-HMAC-SHA256 ...",
    "x-amz-date": "20260520T000000Z",
    "x-amz-content-sha256": "UNSIGNED-PAYLOAD",
    "Content-Type": "image/png",
    "Content-Disposition": "inline"
  }
}
```

## GET /file/sign/upload/document

Returns a PUT signature for a private document.

Query parameters:

| Name | Required | Description |
| --- | --- | --- |
| `filename` | yes | File SHA-256 digest as 64 lowercase or uppercase hex chars. A trailing `.sha256` suffix is accepted and removed. |
| `ext` | yes | One of `pdf`, `doc`, `docx`, `xls`, `xlsx`, `txt`, `md`. A leading `.` is accepted and removed. |

The server checks whether the object already exists in S3. If it exists, `already_uploaded` is `true`; the client can skip uploading.

Response shape is the same as image upload.

## GET /file/sign/access

Returns a presigned GET URL for a private object owned by the current user.

Query parameters:

| Name | Required | Description |
| --- | --- | --- |
| `key` | yes | Object key or full object URL. Only current-user private image/doc keys are allowed. |

Allowed key prefixes:

```text
users/{user_scope}/images/
users/{user_scope}/docs/
```

Response `data`:

```json
{
  "method": "GET",
  "download_url": "http://127.0.0.1:9000/private-bucket/users/{user_scope}/images/{sha256}.png?...",
  "key": "users/{user_scope}/images/{sha256}.png"
}
```

## GET /file/sign/delete

Returns a DELETE signature for an object owned by the current user.

Query parameters:

| Name | Required | Description |
| --- | --- | --- |
| `key` | yes | Object key or full object URL. Current-user avatar, private image, and private doc keys are allowed. |

Allowed keys:

```text
avatars/{user_scope}
users/{user_scope}/images/*
users/{user_scope}/docs/*
```

Response `data`:

```json
{
  "method": "DELETE",
  "delete_url": "http://127.0.0.1:9000/private-bucket/users/{user_scope}/docs/{sha256}.pdf",
  "key": "users/{user_scope}/docs/{sha256}.pdf",
  "headers": {
    "authorization": "AWS4-HMAC-SHA256 ...",
    "x-amz-date": "20260520T000000Z",
    "x-amz-content-sha256": "UNSIGNED-PAYLOAD",
    "Content-Type": null,
    "Content-Disposition": null
  }
}
```
